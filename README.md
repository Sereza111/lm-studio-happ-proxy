# LM Studio + Happ/Xray Proxy

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Запустите **LM Studio** с доступом в интернет через **SOCKS5 прокси Happ** на Windows.

## 🎯 Проблема

LM Studio не видит интернет и не может скачивать модели, даже если консоль работает через прокси Happ/Xray при блокировках.

## ✅ Решение

Используйте батник `run_lm_studio.bat` для запуска LM Studio с переменными окружения SOCKS5 прокси.

---

## 🚀 Быстрый старт

### Шаг 1: Скачайте батник

Просто скачайте файл `run_lm_studio.bat` из этого репо.

Или используйте git:

```bash
git clone https://github.com/Sereza111/lm-studio-happ-proxy.git
cd lm-studio-happ-proxy
```

### Шаг 2: Отредактируйте батник

Откройте `run_lm_studio.bat` в блокноте и замените `YOUR_USERNAME` на ваше имя пользователя Windows.

**Как узнать имя пользователя:**

```powershell
echo $env:USERNAME
```

Например, если выведется `Yozik`, то замените вот это:
```batch
start "" "C:\Users\YOUR_USERNAME\AppData\Local\Programs\LM Studio\LM Studio.exe"
```

На это:
```batch
start "" "C:\Users\Yozik\AppData\Local\Programs\LM Studio\LM Studio.exe"
```

### Шаг 3: Настройте Happ

1. **Откройте Happ**
2. Перейдите в **Настройки → Маршрутизация** (Settings → Routing)
3. **Включите** опцию **"Разрешить подключения из LAN"** (Allow LAN connections)
4. Проверьте **Порт SOCKS5 прокси** (по умолчанию `10808`)

### Шаг 4: Перезагрузитесь

**Это критично!** TUN режим Happ требует перезагрузки для полноценной работы.

```powershell
shutdown /r /t 30
```

### Шаг 5: Запустите LM Studio

После перезагрузки просто **двойной клик на `run_lm_studio.bat`** → LM Studio откроется с доступом в интернет ✅

---

## ✔️ Проверка что работает

1. Откройте **LM Studio**
2. Перейдите в раздел **Models**
3. Если видите список моделей (lfm2, mistral, qwen и т.д.) — **интернет работает!**
4. Попробуйте скачать любую модель для проверки

---

## ❌ Если не работает

### Ошибка: "Error fetching staff picks"

Это означает что интернета всё ещё нет. Попробуйте:

#### 1️⃣ Перезагрузитесь ещё раз

TUN режим требует полной перезагрузки:

```powershell
shutdown /r /t 0
```

#### 2️⃣ Проверьте интернет в консоли

```powershell
ping google.com
```

Если пингуется — значит Happ работает корректно.

#### 3️⃣ Используйте HTTP прокси вместо SOCKS5

Отредактируйте `run_lm_studio.bat` и измените на:

```batch
set HTTP_PROXY=http://127.0.0.1:10809
set HTTPS_PROXY=http://127.0.0.1:10809
set ALL_PROXY=http://127.0.0.1:10809
```

**Обратите внимание:** для HTTP порт обычно `10809`, а не `10808`.

#### 4️⃣ Проверьте другой порт прокси

Если у вас в Happ настроен другой порт — измените в батнике на ваш:

```batch
set HTTP_PROXY=socks5h://127.0.0.1:ВАШИЙ_ПОРТ
set HTTPS_PROXY=socks5h://127.0.0.1:ВАШИЙ_ПОРТ
set ALL_PROXY=socks5h://127.0.0.1:ВАШИЙ_ПОРТ
```

---

## 📋 Требования

- ✅ Windows 10/11
- ✅ Happ (Xray клиент) установлен и запущен
- ✅ LM Studio установлена в стандартное место (`C:\Users\YOUR_USERNAME\AppData\Local\Programs\LM Studio\`)

Если LM Studio установлена в другом месте — измените путь в батнике.

---

## 🎮 Создание ярлыка на рабочем столе

Для быстрого доступа:

1. **Правый клик** на `run_lm_studio.bat`
2. **Отправить → Рабочий стол (создать ярлык)**

Теперь можно просто кликать на ярлык для запуска!

---

## 🔧 Альтернатива: Proxifier

Если батник не работает, используйте [Proxifier](https://www.proxifier.com/):

1. **Скачайте и установите** Proxifier
2. Откройте → **Profile → Proxy Servers → Add**:
   - Address: `127.0.0.1`
   - Port: `10808`
   - Protocol: `SOCKS v5`
   - Сохраните
3. **Profile → Rules → Add Rule**:
   - Application: `LM Studio.exe`
   - Action: выберите свой SOCKS5 прокси
   - Сохраните
4. **Запустите LM Studio обычно** — она будет ходить через прокси

---

## 📚 Как это работает

```
┌─────────────────────────┐
│    LM Studio            │
│  (запущена через        │
│   батник с env vars)    │
└───────────┬─────────────┘
            │
            ↓
┌─────────────────────────┐
│  SOCKS5 прокси          │
│  127.0.0.1:10808        │
│  (переменные окружения)  │
└───────────┬─────────────┘
            │
            ↓
┌─────────────────────────┐
│  Happ/Xray              │
│  (локальный клиент)     │
└───────────┬─────────────┘
            │
            ↓
┌─────────────────────────┐
│  Интернет/Туннель       │
│  (обход блокировок)     │
└─────────────────────────┘
```

- Батник устанавливает переменные окружения `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY`
- LM Studio запускается с этими переменными и использует локальный SOCKS5 прокси Happ
- Весь интернет-трафик от LM Studio проходит через Happ/Xray туннель

---

## 📝 Содержимое батника

```batch
@echo off
REM LM Studio launcher with Happ/Xray SOCKS5 proxy
REM Change YOUR_USERNAME to your Windows username
REM Default Happ ports: SOCKS5=10808, HTTP=10809

set HTTP_PROXY=socks5h://127.0.0.1:10808
set HTTPS_PROXY=socks5h://127.0.0.1:10808
set ALL_PROXY=socks5h://127.0.0.1:10808

start "" "C:\Users\YOUR_USERNAME\AppData\Local\Programs\LM Studio\LM Studio.exe"
```

---

## 📄 Лицензия

MIT License — используйте свободно!

---

## 🤝 Нашли ошибку?

Создавайте Issues с описанием проблемы!

## ⭐ Помогло?

Поставьте звёздочку репозиторию! ⭐

---

**Автор:** [Sereza111](https://github.com/Sereza111)  
**Последнее обновление:** January 2026
